<% provide(:title, 'Цены') %>
<!DOCTYPE html>
<html>
<body>
	<% content = get_preview(@page.content_for(:body)) %>
	<%= raw content %>
	<a href="#" title="Back to top"><%= image_tag("top-btn.png",id:"back-to-top") %></a>
</body>
